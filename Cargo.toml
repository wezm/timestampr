[package]
name = "timestampr"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
home = "0.5.3"
time = { version = "0.3.13", features = ["formatting", "parsing", "local-offset"] }
